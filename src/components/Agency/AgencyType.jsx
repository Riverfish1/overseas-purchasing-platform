import React, { Component } from 'react';
import { Form } from 'antd';

class AgencyType extends Component {
  render() {
    return (
      <div>
        经销商分类
      </div>
    );
  }
}

export default Form.create()(AgencyType);
